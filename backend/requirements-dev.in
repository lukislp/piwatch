# Test/lint/audit tooling used by CI only (not shipped in the image). Constrained to the
# runtime lock so both files resolve to one consistent set of versions.
-c requirements.txt
pytest
pytest-cov
ruff
pip-audit
# Starlette 1.6 deprecates its TestClient over httpx and warns on every test run until httpx2
# is installed.
httpx2>=2.12
